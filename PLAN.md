# PLAN.md — 50분 수직 슬라이스 계획

## 목표

고정 화면에서 플레이어가 이동하고, 자동 공격으로 몰려오는 적을 처치하며, 킬 임계 강화로 더 오래 버티는 Windows 독립 실행 게임을 만든다. 시작 → 생존 → 강화 → 난이도 상승 → 사망 → 재시작의 전체 흐름이 끊김 없이 동작해야 한다.

제품 기능과 수치는 `specs/game-spec.md`가 최종 기준이다. 이 문서는 구현 순서와 시간 운영만 정의한다.

## 기술 기준

- C99, MinGW-w64 GCC
- Win32 API로 창, 메시지, 입력, 고해상도 시간 측정
- GDI 메모리 DC + DIB/Bitmap 백버퍼로 깜빡임 없는 렌더링
- 고정 크기 Player/Enemy/Projectile 데이터와 활성 플래그
- 화면 크기 800×600, 단일 `build/game.exe`
- 외부 에셋과 비시스템 DLL 없음

## 목표 구조

```text
AGENTS.md
GOAL.md
PLAN.md
PACKAGE.md
TEST.md
PROMPT.md
PROGRESS.md
plan/BACKLOG.md
specs/game-spec.md
src/
  main.c
build/
  game.exe
README.md
```

파일 분리는 명확한 이득이 있을 때만 한다. 50분 작업에서는 한두 개의 C 파일이 허용된다.

## 단계별 산출물

### Phase 0 — 00:00~05:00: 첫 실행

- GCC 위치와 버전을 확인한다.
- `build/`을 만들고 명세의 release 명령으로 컴파일한다.
- `WinMain`, 창 클래스, 800×600 클라이언트 영역, 종료 가능한 메시지 루프를 만든다.
- 백버퍼를 생성하고 resize/destroy에서 GDI 객체를 정확히 정리한다.

게이트: 컴파일 exit 0, EXE 존재, 프로세스가 즉시 종료되지 않고 창이 보인다.

### Phase 1 — 05:00~15:00: 움직이는 위협

- `QueryPerformanceCounter` 기반 경과 시간을 사용하고 큰 프레임은 clamp한다.
- 키 상태 배열로 WASD/방향키를 함께 지원하고 대각선 속도를 정규화한다.
- 플레이어를 화면 안에 제한한다.
- Enemy 고정 풀, 화면 가장자리 스폰, 플레이어 방향 추적을 구현한다.
- 플레이어와 적을 색·크기·실루엣으로 즉시 구분되게 그린다.

게이트: 60초 동안 이동과 지속 스폰/추적이 동작하고 풀 범위를 넘지 않는다.

### Phase 2 — 15:00~25:00: 전투 수직 슬라이스

- 가장 가까운 적을 찾아 쿨다운마다 투사체를 자동 발사한다.
- Projectile 고정 풀, 수명/화면 밖 회수, 원형 충돌을 구현한다.
- 적 HP, 투사체 피해, 사망, 처치 수를 연결한다.
- 플레이어 접촉 피해와 0.5초 무적시간을 구현한다.
- 피격/명중은 색 반전, 짧은 플래시, 작은 GDI 파편 중 저비용 방식으로 피드백한다.

게이트: 조작 없이 자동 공격이 적을 죽이고, 접촉은 HP를 감소시키되 한 프레임에 전부 닳지 않는다.

### Phase 3 — 25:00~35:00: 완전한 게임 루프

- 0~30초, 30~60초, 60초 이후의 난이도 3단계를 구현한다.
- 킬 임계값마다 공격력/공격속도를 강화하고 HUD나 순간 텍스트로 알린다.
- HP 0에서 업데이트를 멈추고 결과 화면을 표시한다.
- Space로 모든 런 상태(엔티티, 입력, 타이머, HP, 킬, 강화)를 초기화한다.

게이트: 시작부터 사망과 재시작까지 하나의 프로세스에서 두 번 연속 플레이할 수 있다.

### Phase 4 — 35:00~42:00: 완성도 상승

핵심 게이트가 모두 통과한 경우에만 아래를 위험 대비 효과 순으로 추가한다.

1. HP 게이지, 생존시간, 킬, 강화 단계의 정보 계층 정리
2. 피격 플래시, 명중 파편, 짧은 화면 흔들림
3. 절차적 격자/별 배경과 플레이 영역 테두리
4. 시간대별 적 색/크기 변형(동일 AI 재사용)
5. 스폰 예고 또는 적 등장 펄스
6. 초반/중반/후반 밸런스 조정

게이트: 정보가 겹치지 않고 적·플레이어·투사체·위험이 한눈에 구분된다.

### Phase 5 — 42:00~47:00: 통합 검증

- 최종 후보 EXE로 `TEST.md`의 2분 시나리오를 수행한다.
- BLOCKER → MAJOR → MINOR 순서로 수정한다.
- 재시작 누락, 0벡터 정규화, 배열 한계, 프레임 급증, GDI 객체 누수를 점검한다.
- 정확한 용량과 imported DLL을 확인한다.

게이트: 알려진 BLOCKER/MAJOR가 없고 정확한 payload 총합이 제한 이하이다.

### Phase 6 — 47:00~50:00: 릴리스 동결

- 새 기능을 금지한다.
- 깨끗하게 재빌드하고 동일 EXE에 대해 크기, 의존성, 시작, 수동 핵심 시나리오를 재확인한다.
- `README.md`와 `PROGRESS.md`에 실제 명령과 결과를 기록한다.

게이트: `TEST.md`의 최종 릴리스 레코드가 모두 PASS이다.

## 폴리싱 선택 규칙

각 루프에서 후보를 다음 식으로 판단한다.

```text
우선도 = 사용자 체감 효과 × 성공 확률 / (예상 시간 + 회귀 위험)
```

새 시스템보다 기존 상호작용의 피드백과 밸런스를 우선한다. 한 폴리싱이 5분을 넘기거나 외부 에셋이 필요하면 건너뛴다.

## 축소 순서

시간이 부족하거나 빌드가 불안정하면 아래 순서로 제거한다.

1. 화면 흔들림과 파편 수
2. 배경 장식과 적 외형 변형
3. 강화 알림 애니메이션
4. 복잡한 타이밍/보간 구조

이동, 적 스폰/추적, 자동 공격, 충돌, HP/무적, 킬 강화, 난이도 3단계, 게임오버, 재시작, HUD는 제거하지 않는다.
