# GOAL.md — Codex에 붙여 넣을 시작 명령

아래 코드 블록 전체를 Codex 입력창에 붙여 넣어 Goal을 시작한다. 이 파일 자체를 실행하지 않는다.

```text
/goal `specs/game-spec.md`에 정의된 Windows 네이티브 뱀서라이크 미니게임을 50분 동안 완성하고 지속적으로 품질을 높여라.

## 목표
플레이어 이동, 적 스폰/추적, 자동 공격, 충돌/피해, 킬 기반 강화, 3단계 난이도 상승, 게임오버와 재시작이 연결된 독립 실행형 Windows 게임을 완성한다. 핵심 루프가 완성된 뒤에도 47분까지 테스트, 결함 수정, 피드백, 가독성, 밸런스를 반복 개선한다.

## 먼저 읽을 자료
1. `AGENTS.md`
2. `specs/game-spec.md`
3. `PLAN.md`
4. `plan/BACKLOG.md`
5. `TEST.md`
6. `PACKAGE.md`
7. `PROGRESS.md`

`specs/game-spec.md`를 제품 요구사항의 최종 기준으로 사용하고 수정하지 마라. `참고/`와 `loop.sh`는 실행하지 마라.

## 반드시 작동해야 하는 핵심 기능
1. WASD/방향키 이동과 화면 경계 처리
2. 고정 풀 기반 적 스폰과 플레이어 추적
3. 가장 가까운 적을 향한 자동 투사체 공격과 적 처치
4. 접촉 피해, 0.5초 무적시간, HP와 HUD
5. 킬 임계값 기반 공격력/공격속도 강화
6. 0~30초, 30~60초, 60초 이후 난이도 3단계
7. 게임오버, 생존 결과 표시, Space 완전 재시작
8. 같은 프로세스에서 크래시 없이 2분 이상 플레이

## 제약과 비범위
- C99 + MinGW-w64 GCC + Win32 API + GDI를 유지한다.
- Windows `.exe` 1개를 기본 payload로 하며 전체 실행 파일 합계는 정확히 1,474,560 bytes 이하여야 한다.
- 브라우저, 엔진, 외부 라이브러리/런타임/에셋/네트워크를 사용하지 않는다.
- `specs/game-spec.md`의 MVP 밖 기능은 핵심 기능과 release gate가 통과한 뒤 저위험 폴리싱으로만 다룬다.
- git push, 원격 업로드, 외부 패키지 설치를 하지 않는다.
- 기존에 검증된 동작을 깨뜨리지 않는다.

## 검증 방법
- 빌드: `PACKAGE.md`의 표준 release 빌드 명령
- 크기: payload 총합을 PowerShell로 측정해 `N / 1,474,560 bytes` 기록
- 의존성: objdump/dumpbin으로 imported DLL 확인
- 실행: 최종 `build/game.exe`를 Windows에서 직접 시작해 창과 프로세스 확인
- 시연: `TEST.md`의 기능별 검증과 최종 2분 플레이 시나리오
- 기록: 각 루프의 변경, 실제 검증, 크기, 다음 작업을 `PROGRESS.md`에 기록

## 완료 조건
1. `TEST.md`의 BUILD, STATIC SAFETY, LAUNCH, GAMEPLAY 2 MIN, PACKAGE, DEPENDENCIES가 동일한 최종 EXE에서 모두 PASS다.
2. payload 총합이 1,474,560 bytes 이하이며 정확한 바이트 수가 기록되어 있다.
3. BLOCKER와 MAJOR 결함이 남아 있지 않다.
4. `README.md`에 빌드, 실행, 조작, 구현 기능, 검증 결과, 정확한 EXE 크기가 정리되어 있다.
5. 47분 이전에는 백로그가 끝나도 조기 종료하지 않고 저위험 품질 개선을 계속하며, 47분부터 기능을 동결하고 최종 검증한다.

## 작업 방식
- 시작 시각을 `PROGRESS.md`에 기록하고 `PLAN.md`의 시간 게이트를 따른다.
- 매 Ralph Loop에서 관찰 가능한 작업 하나만 선택해 구현하고 즉시 관련 검증을 수행한다.
- 실패하면 같은 검증이 통과할 때까지 원인을 수정하며 실패 위에 새 기능을 쌓지 않는다.
- 시간이 밀리면 `PLAN.md`의 축소 순서를 적용하되 핵심 루프는 자르지 않는다.
- 사람의 확인 없이도 안전하게 판단 가능한 것은 용량, 안정성, 기존 구조 보존 순으로 자율 결정한다.
- 완료 조건을 모두 증명한 경우에만 성공으로 종료한다.
```
