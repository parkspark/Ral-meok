# AGENTS.md — Ralphthon Codex Goal 하네스

## 임무

Codex Goal이 시작되면 `specs/game-spec.md`의 Windows 네이티브 뱀서라이크를 완성하고, 50분 동안 구현 → 빌드 → 실행 확인 → 수정 → 폴리싱을 반복한다. 최종 실행 payload 전체는 **1,474,560 bytes 이하**여야 한다.

실제 구현 전에 다음 순서로 모두 읽는다.

1. `specs/game-spec.md` — 변경하지 않는 제품 명세
2. `PLAN.md` — 시간대별 우선순위와 축소 규칙
3. `plan/BACKLOG.md` — 현재 실행 가능한 작업 목록
4. `TEST.md` — 주장별 검증 방법
5. `PACKAGE.md` — 정확한 배포 및 용량 규칙
6. `PROGRESS.md` — 이전 루프의 증거와 다음 작업

`참고/`는 설계 근거일 뿐 구현 기준이 아니다. `loop.sh`는 과거 Bash 반복 방식이므로 Goal 실행 중 사용하지 않는다.

## 권한과 보존 규칙

- `specs/game-spec.md`와 `반드시 참고해야할것.txt`는 읽기 전용으로 취급한다.
- 구현 파일, 빌드 파일, `README.md`, `plan/BACKLOG.md`, `PROGRESS.md`는 목표 달성을 위해 수정할 수 있다.
- 사용자의 기존 변경을 지우거나 덮어쓰지 않는다.
- `git push`, 원격 업로드, 패키지 설치, 외부 다운로드를 하지 않는다.
- 로컬 커밋은 필수가 아니다. 남은 시간보다 검증된 실행파일을 우선한다.

## 절대 제약

- C99 + MinGW-w64 GCC + Win32 API + GDI를 유지한다.
- 브라우저, 게임 엔진, SDL, 외부 라이브러리, 외부 런타임을 사용하지 않는다.
- 이미지·폰트·사운드 파일을 추가하지 않는다. 화면은 GDI 도형과 Windows 기본 글꼴로 그린다.
- 적과 투사체는 고정 크기 배열/오브젝트 풀로 관리한다. 프레임 루프에서 동적 할당하지 않는다.
- 최종 payload는 기본적으로 `build/game.exe` 한 파일이며 총합이 1,474,560 bytes 이하여야 한다.
- 명세의 MVP 밖 기능은 핵심 루프와 릴리스 게이트를 모두 통과한 뒤에만 폴리싱 후보로 다룬다.

## 한 번의 Ralph Loop

한 루프는 2~5분 안에 검증 가능한 작업 하나만 끝낸다. 단, 실패한 검증을 고치는 일은 같은 작업에 포함한다.

1. `PROGRESS.md`에서 시작 시각, 경과 시간, 마지막 증거, 열린 BLOCKER를 확인한다.
2. 현재 빌드가 있으면 크기와 빌드 상태를 먼저 확인한다.
3. `plan/BACKLOG.md`에서 가장 위의 미완료 작업 하나를 선택한다.
4. 해당 작업의 관찰 가능한 완료 조건을 한 문장으로 정한다.
5. 기존 구조를 유지하는 최소 변경으로 구현한다.
6. `TEST.md`의 관련 집중 검증과 기본 빌드를 수행한다.
7. 실패하면 원인을 좁혀 같은 검증을 다시 수행한다. 실패 상태 위에 새 기능을 쌓지 않는다.
8. 성공 증거가 있을 때만 BACKLOG를 체크한다.
9. `PROGRESS.md`에 아래 형식으로 기록하고 즉시 다음 루프로 넘어간다.

```text
## Loop N — MM:SS — DONE | BLOCKED
작업: T-XX 작업명
변경: 사용자에게 보이는 결과 또는 수정 파일
검증: 실제 명령/수동 시나리오와 결과
크기: N / 1,474,560 bytes
판단: 범위 축소나 기술 판단이 있을 때 근거
다음: 다음 최우선 작업 또는 blocker 해소책
```

## 시간 운영

- 00:00~05:00 — 도구 확인, 빌드, 창, 입력 가능한 첫 실행
- 05:00~15:00 — 이동, 적 풀, 스폰, 추적까지 생존 압력 완성
- 15:00~25:00 — 자동 사격, 충돌, 처치, 플레이어 피해
- 25:00~35:00 — 난이도 3단계, 킬 기반 강화, 게임오버/재시작
- 35:00~42:00 — HUD, 피드백, 밸런스, 가독성
- 42:00~47:00 — 2분 통합 플레이, 결함 수정, 용량/의존성 감사
- 47:00~50:00 — 기능 동결, 최종 빌드와 정확한 EXE 검증

시간 게이트가 지났는데 앞 단계가 미완료면 폴리싱을 건너뛰고 핵심 루프를 먼저 연결한다. 백로그가 일찍 끝나도 조기 종료하지 말고 47분까지 결함, 조작감, 가독성, 밸런스를 검증하며 개선한다. 47분 이후에는 BLOCKER와 릴리스 검증만 수행한다.

## 의사결정 원칙

판단이 필요하면 질문을 기다리며 멈추지 않고 아래 순서로 선택한다.

1. 실행 가능성과 크래시 방지
2. 명세의 전체 플레이 루프 연결
3. 1,474,560-byte 제한
4. 기존 코드 변경량 최소화
5. 눈에 보이는 게임 완성도

결정 근거는 `PROGRESS.md`에 한 줄 남긴다. 요구사항이 정말로 양립 불가능하거나 새 권한이 필요한 경우에만 사용자 입력을 요청한다.

## 실패와 회복

- 컴파일 실패: 같은 루프에서 첫 오류부터 수정하고 재빌드한다.
- 실행 즉시 종료/창 미표시: BLOCKER로 분류하고 새 기능을 중단한다.
- 수동 조작 확인 불가: 자동으로 가능한 빌드·프로세스·정적 검증을 수행하고, 검증하지 못한 항목을 PASS로 쓰지 않는다.
- 3회 연속 같은 BLOCKER: 시도와 오류 원문을 기록하고 우회 구현으로 범위를 축소한다. 권한/도구 부재로 우회도 불가능할 때만 사용자에게 알린다.
- 용량 초과: `PACKAGE.md`의 감량 순서를 즉시 적용한다. 마지막 3분까지 미루지 않는다.

## 완료 정의

`TEST.md`의 최종 게이트가 동일한 최종 EXE에 대해 모두 PASS이고, `README.md`에 빌드·실행·조작법·구현 기능·정확한 바이트 수가 기록된 경우에만 성공이다. 컴파일만 성공했거나 실행/게임플레이 증거가 없으면 PARTIAL로 보고한다.
